spring.application.name=backend
server.port=8080
app.cors.allowed-origin=http://localhost:5173
# Without this, Spring's default error body omits the exception message
# (ResponseStatusException's reason) for security reasons — we want it,
# since this API has no sensitive internals to leak.
spring.web.error.include-message=always
